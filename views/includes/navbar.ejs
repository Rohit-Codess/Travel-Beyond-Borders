<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top shadow-sm py-3">
  <div class="container-fluid px-4" style="background-color: #c8d8e4">
    <!-- Logo and Branding -->
    <a class="navbar-brand fw-bold text-success d-flex align-items-center fs-4" href="/">
       
      <span class="mx-2 text-danger">🌍 Beyond</span>
      <span>Borders</span>
    </a>

    <!-- Toggler -->
    <button
      class="navbar-toggler border-0"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Nav Items -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav gap-3 fw-semibold">
        <li class="nav-item">
          <a class="nav-link text-dark" href="/listings">Listed Places</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-dark" href="/listings/new">Share You Destination</a>
        </li>

        <!-- Account Dropdown -->
        <li class="nav-item dropdown">
          <a
            class="nav-link dropdown-toggle text-dark"
            href="#"
            id="userDropdown"
            role="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            Account
          </a>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
            <% if (!currentUser) { %>
              <li><a class="dropdown-item" href="/users/signup">Sign Up</a></li>
              <li><a class="dropdown-item" href="/users/login">Log In</a></li>
            <% } else { %>
              <% if (currentUser.isAdmin) { %>
                <li><a class="dropdown-item" href="/admin/users/dashboard">🧑‍💻 Admin Panel</a></li>
              <% } else { %>
                <li>
                  <span class="dropdown-item text-muted small">
                    👤 <%= currentUser.username || currentUser.email %>
                  </span>
                </li>
              <% } %>
              <li><a class="dropdown-item" href="/users/settings">⚙️ Settings</a></li>
              <li><hr class="dropdown-divider" /></li>
              <li>
                <a class="dropdown-item text-danger" href="/users/logout"
                   onclick="return confirm('Do you want to Log out 😊')">
                   ⏻ Logout
                </a>
              </li>
            <% } %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
